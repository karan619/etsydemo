
<div class="jumbotron">
  <h2>Hand Crafted items from around the world!</h2>
  <h3>One-of-a-kind.</h3>
</div>

<div class="row text-center">
  <% @listings.each do |listing| %>
    <div class="col-md-3">
    <%= link_to listing do %>
      <div class="thumbnail">
        <%= image_tag listing.image.url %>
        <div class="caption">
          <h3><%= listing.name %></h3>
    <% end %>
          <p><%= number_to_currency(listing.price, unit: "₹") %></p>
          <p><%= "Sold by #{listing.user.name}" %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>

<br />

<%= link_to 'New Listing', new_listing_path, class: "btn btn-link" if user_signed_in? %>

